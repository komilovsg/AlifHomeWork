// function countProperties(obj) {
//   return Object.keys(obj).length;
// }
const countPropertiesObj = (obj) => Object.keys(obj).length;

const user = { name: "Alice", age: 30, city: "New York" };
console.log(countProperties(user));
